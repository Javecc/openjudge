#include<stdio.h>
#define MAXLEN 101
int simulate()   //模拟爆炸
{

}

int main(void)
{
    int n,i,j;
    int location[MAXLEN];
    int now;       //当前的坐标点
    int maxnum = 1;    //最大爆炸数，初始化为1
    scanf("%d",&n);
    for(i=0;i<n;i++){
        scanf("%d",&a[i]);
    }
    for(j=0;j<n;j++){

    } 
    return 0;
}